{
    "scripts": {
        "pretest": "ts-node --transpile-only --ignore=false test/pretest.ts",
        "test": "ts-node --transpile-only --ignore=false test/test.ts",
        "dfx-start": "dfx start --background --clean",
        "dfx-stop": "dfx stop",
        "start": "dfx deploy",
        "start:dfx-restart": "dfx stop && dfx start --background --clean && dfx deploy"
    },
    "dependencies": {
        "azle": "0.21.1",
        "cbor": "^9.0.2"
    },
    "devDependencies": {
        "@dfinity/agent": "^0.19.2",
        "ts-node": "^10.9.1",
        "typescript": "^5.2.2"
    }
}
